#include "game.h"
#include "input.h"

#include <iostream>


void Game::init( ){
        /*
        this->core = core;
        this->core->init();
        this->graphics = graphics;
        this->graphics->init();
        Keyboard::init(core->getKeyState());
        
        
        graphics->loadTexture("img/test.png");
        player.init();
        player.sprite.texture.set(graphics->getTexture("img/test.png"));
        player.setPosition(Vector2(100,100));
        */
}

 void Game::run(){
        /*
        while(core->isRunning()){
                
                
            
        }*/
    }

void Game::update(){
       //player.update(0);

}

void Game::draw(){
      /*  graphics->clearScreen(100,10,0);
        player.draw(graphics);
        //graphics->drawSprite(100,100,texture.get());

        graphics->flipDisplay();*/
}

void Game::quit(){
       /* graphics->quit();
        core->quit();*/
}